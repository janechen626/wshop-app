export default {
    userAddress: state => state.userAddress,
    addtype: state => state.addtype,   //进入地址的类型
    orederAddress: state => state.orederAddress,   //进入地址的类型
    couponList: state => state.couponList,   //获取可以使用的优惠券 参数
    coupon: state => state.coupon,   //获取可以使用的优惠券 参数
}
